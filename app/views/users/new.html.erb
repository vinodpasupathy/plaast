<script>
$(document).ready(function() {
    $('#submit').click(function(event){
    
        data = $('.password').val();
        var len = data.length;
        
        if(len < 1) {
            alert("Fields cannot be blank");
            // Prevent form submission
            event.preventDefault();
        }
         
        if($('.password').val() != $('.confpass').val()) {
            alert("Password and Confirm Password don't match");
            // Prevent form submission
            event.preventDefault();
        }
         
    });
});
</script>

<div class="bod">
    <div class="col-md-10 col-sm-8 body-wrap">
        <div class="set">
   <div class="spc1"> 
    <div class="col-md-5">
    <div class="body-container">
                  <div class="body-header">
                      Registration Page
                    </div>
                    <div class="body-content">
                        <div class="table-container">
<table class="table">
<tbody>
<%= form_for User.new do | i | %>
<tr>
<td><%= i.label :name%></td>
<td><%= i.text_field :name,:step => "any",:required => true,:autofocus=>true%></td>
</tr>
<tr>
<td><%= i.label :password%></td>
<td><%= i.password_field :password,:class=>"password",:step => "any",:require=>true %></td>
</tr>
<tr><td><%= i.label :password_confirmation,"Password Confirmation"%></td>
<td><%= i.password_field :password_confirmation, :value => params[:password],:class=>"confpass",:step => "any",:require=>true %></td></tr>
<tr><td><%= i.label :role,"Role"%></td>
<td><%= i.select :role, options_for_select([["Admin"],["Operator"],["Factory"]]),:prompt=> '--select--'%></td></tr>
<tr><td colspan="2">
<center><%= i.submit "Register",:id=>"submit",:class=>"btn btn-info"%></center>
</td></tr>
<%end%>
</form>
</tbody>
</table>
</div>
</div>
</div>
</div>
</div>

 <div class="spc">
<div class="col-md-5">
<div class="body-container">
<div class="body-header">
User Details
</div>
<div class="body-content">
<div class="table-container">
<table class="table">
<thead>
<tr>
<th>User Name</th>
<th>Role</th>
<th>Action</th>
</tr>
</thead>
<% User.all.each do|i| %>
<tbody>
<tr class="odd">

<td><%= i.name %></td>
<td><%= i.role %></td>
<td><%= link_to image_tag("/assets/trash1.png", height: '20', width: '20',:class=>"img-circle",:title => 'Delete', :rel => 'tooltip',:onclick=>"if (confirm('Are you sure!!!')) commentDelete(1); return false"),{:action => 'user_destroy', :id => i.id }%></td>
</tr>
</tbody>
<%end%>
</table>
</div>
</div>
</div>
</div>
</div>
</div>
</div>
