<div class="bod">
  <div class="col-md-10 col-sm-8 body-wrap">
<div class="set">
<div class="body-container">
<div class="body-header">
Machine Used
</div>
<div class="body-content">
<div class="table-container">
<div class="header">
    List of Machine Used
</div>
<table class="table">
    <thead>
<tr>
<th>Used Machines</th>
<th>Action</th>
</tr>
</thead>
<%= form_for([@machine_used], validate: true, :url=>{:controller=>"users", :action => "machine_use_create"}) do |i|%>
<tbody>
<tr><td><%=i.text_field:machine_used_list,:required => true%>
<td align="center"><%= i.submit "Add",:class=>"btn btn-info"%>
 <%end%></td></tr>

<% @machine_used1.each do |i| %>
<tr class="odd"><td><div class="fot"><%= i.machine_used_list%></div></td>
<td><%= link_to image_tag("/assets/trash1.png", height: '20', width: '20',:class=>"img-circle",:title => 'Delete', :rel => 'tooltip',:onclick=>"if (confirm('Are you sure!!!')) commentDelete(1); return false"),{:action => 'machine_use_delete', :id => i.id }%></td>

</tr>
</tbody>
<%end%>
</table>
</div>
</div>
</div>

<div class="digg_pagination" align="right">
    <%= will_paginate @machine_used1, :container => false %>
 </div>
</div>
</div>
